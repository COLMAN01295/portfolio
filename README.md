SMTP relay server for portfolio contact form

1. Copy `.env.example` to `.env` and fill in your SMTP credentials.

2. Install dependencies:

```bash
npm install
```

3. Start the server:

```bash
npm start
```

4. Open `http://localhost:3000` and submit the contact form. The server will send emails using your SMTP provider.
